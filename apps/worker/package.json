{
  "name": "@refi-radar/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@refi-radar/shared": "workspace:^",
    "hono": "^4.12.17"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260506.1",
    "wrangler": "^4.88.0"
  }
}
